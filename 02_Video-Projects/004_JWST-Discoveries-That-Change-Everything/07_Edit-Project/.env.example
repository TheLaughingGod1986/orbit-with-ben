# Native Gemini Veo API — FALLBACK ONLY (NOT default CG path)
# Default CG: AI Studio Ultra UI → python3 04_Audio/tools/orbit_aistudio_veo_ui.py --login
# This API key path is billed separately from Google One Ultra — avoid for routine clips.
# GEMINI_API_KEY=
