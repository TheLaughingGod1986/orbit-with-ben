---
description: Orbit CG via Google Flow Veo UI (Ultra) — ElevenLabs is VO-only
alwaysApply: true
---

# Orbit — CG generation = Google Flow Veo UI / Ultra (locked)

**Picture (CG / motion clips):** **Google Flow Veo UI** via Playwright — uses **Google One → AI Ultra** Flow credits  
**Narration (VO):** **ElevenLabs TTS only** — Ben Orbit Narrator (`kDch6ACCIpqgQ0NsU9kk`)

## Do this

1. Generate CG with `04_Audio/tools/orbit_flow_veo_ui.py` (or episode wrappers / `_generate_veo_from_beats.py`).
2. Auth: one-time `python3 orbit_flow_veo_ui.py --login` on the Ultra Google account (persistent profile — no API key for routine CG).
3. Agent settings: **Never** confirm · **Veo 3.1 - Fast** · 16:9 · attach Orbit reference · silent prompt via `orbit_gemini_veo.build_prompt()`.
4. **Strip audio** after download (`strip_cg_native_audio.py` / helpers).
5. Mix British VO later from ElevenLabs — never use Veo speech.

## Do not

- Use **`GEMINI_API_KEY` / Gemini API Veo** as the default path — API is billed separately and is usually more expensive than Ultra Flow for episode volume. API helper (`orbit_gemini_veo.py`) is **last-resort fallback**.
- Prefer **AI Studio Veo UI** over Flow for routine CG — Studio often still demands a **paid API key** even with Ultra. Keep `orbit_aistudio_veo_ui.py` as secondary only.
- Use **ElevenLabs Image & Video** (Omni / Veo Fast in the EL UI) for new CG — costly, Explore/Eiffel contamination, American speech.
- Treat Veo/Omni native speech as channel VO.
- Skip the Growth System v2 gate (vidIQ + script ≥90) before spending Ultra / Veo credits.

## Legacy

Existing `_generate_omni_*.py` / Playwright EL Image-Video scripts are **legacy**. Prefer Google Flow Ultra UI for all new episodes and regenerations.

Canonical helper: `04_Audio/tools/orbit_flow_veo_ui.py`  
Shared prompt lock: `04_Audio/tools/orbit_gemini_veo.py` (`build_prompt`)  
Docs: `docs/GEMINI_VEO_CG.md`  
VO lock: `.cursor/rules/orbit-british-vo-lock.mdc` · `04_Audio/tools/orbit_voice.py`
